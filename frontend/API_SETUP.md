### API Configuration
- **Base URL**: `http://127.0.0.1:8088/api/auth`
- **Endpoints**:
  - POST `/login`
  - POST `/register`

### Requirements
- Backend MUST be running on port 8088
- Backend MUST expose `/api/auth/login` and `/api/auth/register`
- Requests MUST send JSON body
